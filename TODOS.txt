[Fixes]
1. Fix readMe to IRS(using gemini)


[Improvments]
2. Add more advanced behaviors in Gsim.
3. Add client Turn on even when the server doens't exist(it will keep trying to reconnect until the server turns on make its color YELLOW to show the connection is turned on but not connected)


[TODOS]
1. read the whole project and enforcing Clean code and understandable code.
2. Get GitKraken.
