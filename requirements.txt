# ==============================================================================
# Full pinned dependency set for `core` + `gsim`, including every transitive
# package -- captured from a verified-working environment (.venv3.10, all 178
# tests passing) so this list can be resolved once on a machine with internet
# access and mirrored for offline/closed-LAN installs, e.g.:
#
#   pip download -r requirements.txt -d wheelhouse/
#   pip install --no-index --find-links wheelhouse -r requirements.txt
#
# Python: >=3.10 (see pyproject.toml)
# ==============================================================================

# ---- core: IRS engine, connections, tools -----------------------------------
beartype==0.22.9

# ---- gsim: FastAPI/Uvicorn HTTP API ------------------------------------------
fastapi==0.141.1
starlette==1.6.0
pydantic==2.13.4
pydantic_core==2.46.4
annotated-types==0.8.0
annotated-doc==0.0.5
typing_extensions==4.16.0
typing-inspection==0.4.4

uvicorn==0.52.4
click==8.4.2
h11==0.16.0
colorama==0.4.6            # Windows console colors (click / pytest)

# uvicorn[standard] extras actually resolved in the working env
anyio==4.14.2
idna==3.19
exceptiongroup==1.3.1      # anyio's backport, needed on py<3.11
httptools==0.8.0
python-dotenv==1.2.3
PyYAML==6.0.3
watchfiles==1.2.0
websockets==16.1.1

# ---- gsim: native desktop window (GUI mode; not needed for --server) --------
pywebview==6.2.1
pythonnet==3.1.0           # pywebview's Windows WinForms/Edge backend
clr_loader==0.3.1          # pythonnet's CLR loader
cffi==2.1.1
pycparser==3.0
proxy_tools==0.1.0
bottle==0.13.4             # pywebview's bundled local HTTP server

# ---- dev / test ---------------------------------------------------------------
pytest==9.1.1
iniconfig==2.3.0
pluggy==1.6.0
Pygments==2.21.0
tomli==2.4.1                # pytest config parsing on py<3.11

# ---- building the `core` wheel (PEP 517, see pyproject.toml) ------------------
build==1.5.0
pyproject_hooks==1.2.0
setuptools==83.0.0
packaging==26.3

# ---- optional, NOT installable from PyPI ---------------------------------------
# rti.connext==7.7.0
#   RTI Connext DDS Python API, only needed for core.connections.dds (DDS
#   transport). It ships with your RTI SDK installer, not a public package --
#   on a closed LAN you must copy its wheel/site-packages dir from a machine
#   with the RTI installation rather than fetching it here. Everything else in
#   core.connections works fully without it (see core/connections/__init__.py,
#   which catches ImportError and disables DDS gracefully).
