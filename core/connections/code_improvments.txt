1. Where do we use base.py - class Unit(Protocol)
2. _EventLoopThread onky has one class instance right? so why do we do we define loop as self.loop in the _start and not in the class attributes like _instance?
3. self._state_event is only triggred when shutting down / when a new connection is established right? so lets call it new_connection_event?
4. Currently for a client(TCP) we cant set a local_port it will always be port = 0.
5. in tcp.py under def _on_peer_lost add writer.close() to close a dying/bad socket
6. in tcp.py we can just initialize self._write_lock as asyncio.Lock() instead of doing it in def _do_start(), then deleting assert self._write_lock is not None.
7. we should probably change name ip to remote_ip.
8. in udp.py def _do_send() if peer is None the code under is dead code since if peer is None we do not define the remote addr.
9. in multicast.py define the interfaces that will sends the multicast traffic.
10.
