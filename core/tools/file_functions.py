
def read_unit_config(file_path: str) -> dict:
    """ gets a file path resolve the current working directory of the caller
        and joins the two path's of this path doesn't exist
        is checks the origin file path if this doesn't exist raises an error.
    """
    ...
